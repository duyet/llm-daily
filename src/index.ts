/**
 * LLM Daily - Main entry point
 * @packageDocumentation
 */

export const VERSION = '0.1.0';

/**
 * Get application version
 * @returns Current version string
 */
export function getVersion(): string {
  return VERSION;
}
